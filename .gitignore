node_modules
.next
out
.env*.local
.DS_Store
*.tsbuildinfo
next-env.d.ts
.turbo

# Embedded Postgres data directory (PGlite). Real database contents — accounts, password
# hashes, orders. Never committed.
/.data/

# Scratch space for one-off scripts. Never part of the product.
/.tmp/
